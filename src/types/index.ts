// Interface for the formated inputs from the form - dateOfBirth and startDate are formated to string type
export interface InputsFormated {
    firstname: string;
    lastname: string;
    dateOfBirth: string;
    startDate: string;
    street: string;
    city: string;
    state: string;
    zipCode: string;
    department: string;
}
